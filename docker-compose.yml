version: '3.8'

services:
  nginx:
    image: nginx:alpine
    container_name: streamlit-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./logs/nginx:/var/log/nginx
      - ./ssl:/etc/nginx/ssl:ro
    networks:
      - streamlit-network
    restart: unless-stopped
    runtime: runc
    depends_on:
      - sample-app

  sample-app:
    image: python:3.11-slim
    container_name: streamlit-sample-app
    working_dir: /app
    volumes:
      - ./apps/sample-app:/app
    command: >
      sh -c "pip install streamlit pandas numpy matplotlib seaborn &&
             streamlit run app.py --server.port=8501 --server.address=0.0.0.0 --server.headless=true"
    networks:
      - streamlit-network
    restart: unless-stopped
    runtime: runc
    expose:
      - "8501"

  n2s-tmmi-tracker:
    image: python:3.11-slim
    container_name: streamlit-n2s-tmmi-tracker
    working_dir: /app
    volumes:
      - ./apps/n2s-tmmi-tracker:/app
    command: >
      sh -c "pip install -r requirements.txt &&
             streamlit run app.py --server.port=8501 --server.address=0.0.0.0 --server.headless=true"
    networks:
      - streamlit-network
    restart: unless-stopped
    runtime: runc
    expose:
      - "8501"

  n2s-impact-model:
    image: python:3.11-slim
    container_name: streamlit-n2s-impact-model
    working_dir: /app
    volumes:
      - ./apps/n2s-impact-model:/app
    command: >
      sh -c "pip install -r requirements.txt &&
             streamlit run app.py --server.port=8501 --server.address=0.0.0.0 --server.headless=true"
    networks:
      - streamlit-network
    restart: unless-stopped
    runtime: runc
    expose:
      - "8501"

  n2s-estimator:
    image: python:3.11-slim
    container_name: streamlit-n2s-estimator
    working_dir: /app
    volumes:
      - ./apps/n2s-estimator:/app
    command: >
      sh -c "pip install -r requirements.txt &&
             streamlit run src/n2s_estimator/ui/main.py --server.port=8501 --server.address=0.0.0.0 --server.headless=true"
    networks:
      - streamlit-network
    restart: unless-stopped
    runtime: runc
    expose:
      - "8501"

networks:
  streamlit-network:
    driver: bridge

volumes:
  nginx-logs:
